AIRFLOW_UID=1000                  # echo "AIRFLOW_UID=$(id -u)" > .env
AIRFLOW_GID=1000                  # echo "AIRFLOW_GID=$(id -g)" > .env
DOCKER_GID=988                    # echo "getent group docker | cut -d: -f3" > .env
HOST_USERNAME=ubuntu              # echo "HOST_USERNAME=$(whoami)" > .env 
FERNET_KEY=KEY                    # paste key (DO NOT USE AUTO GENERATE OR SCRIPT GENERATE)
